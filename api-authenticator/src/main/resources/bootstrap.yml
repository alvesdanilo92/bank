spring:
  application:
    name: api-authenticator
    serviceId: api-authenticator
  jackson:
    default-property-inclusion: NON_EMPTY
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev,logbook}
  flyway:
    enabled: false
    baseline-on-migrate: true